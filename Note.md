# Fix "property_list" api

-   we should be able to use this api to return is_favorite = true for the different properties,
    to use that data in the frontend to make mark the wishlist button as selected
